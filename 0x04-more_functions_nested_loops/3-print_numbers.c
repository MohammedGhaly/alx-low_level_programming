#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two numbers.
 *
 *@a: the first digit
 *
 *@b: the second digit
 *
 * Return: the multiplication of a * b.
 */
void print_numbers(void)
{
	int i = 0;

	while (i < 10)
	{
		_putchar(i + 48);
		i++;
	}
}
