#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two numbers.
 *
 *@a: the first digit
 *
 *@b: the second digit
 *
 * Return: the multiplication of a * b.
 */
int mul(int a, int b)
{
	return (a * b);
}
